version: "3.9"
services:
  voicevibe:
    build: .
    volumes:
      - ./:/app
      - ./instance:/app/instance
      - ./uploads:/app/uploads
    ports:
      - "5000:5000"
    environment:
      - FLASK_HOST=0.0.0.0
      - FLASK_PORT=5000
